import { BanchProgressData } from './banch-progress-data';

describe('BanchProgressData', () => {
  it('should create an instance', () => {
    expect(new BanchProgressData()).toBeTruthy();
  });
});
